#include <stdio.h>

int main() {

    int num;

    scanf("%d", &num);

    if (num >= 10 && num <= 99) {

        int tens = num / 10;

        int units = num % 10;

        int swapped = units * 10 + tens;

        printf("%d\n", swapped);

    }

    return 0;

}
