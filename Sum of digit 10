#include <stdio.h>

int main() {

    int n, sum = 0;

    scanf("%d", &n);

    if (n <= 99) {

        printf("Invalid Input\n");

        return 0;

    }

    while (n > 0) {

        sum += n % 10;

        n /= 10;

    }

    printf("Sum of digit is %d\n", sum);

    return 0;

}
