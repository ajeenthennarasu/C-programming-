#include <stdio.h>

int main() {

    int n, i, num;

    float sum = 0;

    scanf("%d", &n);

    for (i = 0; i < n; i++) {

        scanf("%d", &num);

        sum += num;

    }

    printf("Array Mean Value is %.2f\n", sum / n);

    return 0;

}
